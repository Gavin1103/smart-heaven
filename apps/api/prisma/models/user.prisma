model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String // TODO: hash password
  firstName String
  lastName  String
  role      Role
  createdAt DateTime @default(now())
}

enum Role {
  ADMIN
  USER
}
